# 삼성흉부외과 대전 — 작업 기록

## [2026-08-03 #1] MVP 구축 — 크롤 · 디자인 확정 · 배포

### 지금 상태 (내일 여기서 시작)

- **라이브**: https://veindoc-daejeon.vercel.app (검색엔진 차단 중, URL 아는 사람만)
- **코드**: `D:\Colin 작업폴더\veindoc-mvp` (Next.js 16.2.12 / React 19 / TS)
- **Vercel**: 팀 `samsung-clinic`, 프로젝트 `veindoc-mvp`, 계정 `colinparkh1-6604`
- **Supabase**: `https://grjptglaektlcoyhkdup.supabase.co`
- **원본 미러**: `D:\Colin 작업폴더\삼성흉부외과 사이트\site` (veindoc.co.kr 전체)
- **지점 참고 미러**: `…\_지점_veindoc.kr(안양범계)` (신형 디자인 참고용)

---

## 1. 원본 사이트 크롤 (완료)

- veindoc.**co**.kr = 진짜 대상 (XE, EUC-KR, HTTPS). veindoc.kr 은 **안양/평촌 지점**의 다른 사이트.
- `site/` 4,582 파일 / 184MB. 다국어(en/jp/cn)·모바일(/m) 포함.
- 게시판 전량: 치료후기 942, 진료상담 2,457.
- 재크롤: `삼성흉부외과 사이트/_tools/crawl.py`

### 원본 사이트에서 확인된 사실
- HTTPS 정상, `www` 강제. 진료시간이 **텍스트로 존재하지 않음**(만화 삽화뿐) → 현재 값은 가정치.
- 제목·진료시간 등이 이미지로 박혀 있어 SEO 손실 큼.
- 반응형 없음(viewport 메타 없음), 별도 `/m` 34페이지 이중관리.

---

## 2. 치료후기 데이터 (완료)

| 항목 | 값 |
|---|---|
| 크롤 원본 | 942건 |
| **봇 더미 스팸** | **768건** — 제목·작성자·본문이 전부 `88952634` 한 종류. 2006~2012에 717건 집중 |
| 본문 없는 글 | 12건 |
| **실제 후기** | **162건** (Supabase 적재 완료) |

- **대구 언급 0건** (대구는 스팸 글에만 있었음). **청주 1건**은 "청주 거주 → 천안 지점 치료" 후기라 유지.
- 원본이 **대전·평촌·천안 3지점 공용 게시판**이고 글별 지점 구분이 없음 → 지점 필터 제거함.
- DB `branch` 컬럼은 전부 `대전`으로 들어가 있음(무의미). 정리하려면:
  ```sql
  update public.reviews set branch = '네트워크';
  ```
- 재이관: `scripts/import_reviews.mjs` (secret 키 필요, 실행 후 즉시 폐기할 것)

---

## 3. 디자인 확정 경위 (v1 → v10)

시안 파일: `public/demo.html` ~ `demo10.html` (**demo10이 확정본**)

### 사용자가 반려한 것과 이유
| 버전 | 시도 | 반려 사유 |
|---|---|---|
| v1~v2 | 크로스페이드 슬라이드쇼 + 혈류 SVG | 저화질·잘림·칙칙·"예전 방식"·혈류는 인식 안 됨 |
| v3~v5 | 책등 서가 / FLIP 확장 | "병원 느낌 안 남" |
| v6 | 표지 사진 서가 | 개선됐으나 부족 |
| **v7** | **밝은세상안과 레시피 적용** | 방향 맞음 |
| v9~v10 | 메니스커스 + 아버지 증거 | **확정** |

### 핵심 교훈
- **우영미(패션) 톤을 병원에 그대로 이식한 게 근본 실수.** 병원 히어로는 정반대 — **Pretendard 80px / weight 600 + 큰 숫자**.
- 밝은세상안과 실측: 자동재생 루프 영상, 제목 80px/600, 딤 오버레이 없음, 미디어 1.06배 + 마우스 패럴랙스, 슬라이드 7개.
- 아버지 슬라이드는 **1383×401 배너**라 풀스크린에 넣으면 2.7배 확대 → 반드시 축소 표시(현재 0.97배).

---

## 4. 아버지가 보존을 원한 자산 (절대 손대지 말 것)

`public/hero/` 원본 5장, **무보정·무크롭**:

| 파일 | 내용 | 방울 색 |
|---|---|---|
| slide_img4.jpg | Why 삼성흉부외과 (40,000 Case) | `#0070BC` 블루 204° |
| slide_img20191004.jpg | 미국 정맥학회 이사 캐더린 깁슨 협진 | `#C8102E` 레드 350° |
| slide_img3.jpg | 일본 정맥학회장 호시노 교수 방문 | `#04A33F` 그린 142° |
| slide_img1.jpg | 리얼스토리(여성 환자) | `#7A3FD1` 퍼플 264° |
| slide_img2.jpg | 리얼스토리(남성 환자) | `#F09000` 오렌지 36° |

> 학술 교류 사진은 **실제 기록**이라 AI 재생성 금지. 색상환 최소 간격 46°로 명확히 구분.

---

## 5. 구현된 것

- **홈**: 메니스커스 액체 바(Instagram MENISCUS 릴스 응용). 방울을 끌면 바가 녹았다 솟고 증거가 바뀜. FLIP 전체화면 뷰어.
- **Deck**: PPT형 슬라이드(스크롤 없음). 키보드·휠·스와이프. 화살표는 **좌우 가장자리**. 마지막 슬라이드에서만 휠 스크롤 허용(푸터 도달용).
- **탭 6종**: `/vein`(5) `/treatment`(6) `/reviews` `/academic`(5) `/guide`(4) `/location`(2)
- **후기**: Supabase Auth 회원 전용(의료법상 체험담 열람 제한). RLS로 DB 레벨 차단.
- **푸터**: 3지점 사업자정보 원본 그대로.
- **퀵메뉴**(우측): 온라인예약·진료상담·리얼스토리·치료가상체험 / 약도 문자받기 / 3지점 전화.
- **네이버 톡톡**: 헤더 1개. 녹색 `#03C75A` + 말풍선 + 입력중 점3개 + 맥박링 + 7초 넛지.

### 3지점 실제 정보 (원본 nintro/0207 확인)
| 지점 | 대표 | 사업자번호 | 전화 |
|---|---|---|---|
| 대전 | 차대원 | 314-91-03689 | 042-471-3075 |
| 평촌 | 김성철 | 138-90-56590 | 031-382-7588 |
| 천안 | 문상호 | 312-91-30725 | 041-564-8877 |

---

## [2026-08-03 #2] 헤더 CTA · 푸터 · 퀵메뉴 · 모바일 이미지

### 추가된 것
- **헤더 CTA 2종**: 자가체크(퍼플 `#6D31C4`) + 네이버 톡톡(그린 `#03C75A`).
  - 모션 대칭: 톡톡=말풍선+입력중 점3개 / 자가체크=클립보드+체크 3개 순차 드로잉. 각각 맥박링 + 7초 넛지(오프셋 4.5s로 동시 발동 방지).
  - `SelfCheck` 를 전역 컴포넌트로 분리 → `layout.tsx` 에 마운트, 헤더 버튼이 `open-selfcheck` 커스텀 이벤트로 연다. 전 페이지에서 동작.
  - **TODO: 구글 설문지 연동** — `SelfCheck.tsx` 의 `상담 문의하기` 버튼.
- **SiteFooter**: 3지점 사업자정보(원본 그대로) + 하단 링크 + NAVER/TISTORY/SNS.
- **QuickRail**(우측): 온라인예약·진료상담·리얼스토리·치료가상체험 / 약도 문자받기(지점선택+전화입력) / 3지점 전화. 모바일은 우하단 토글.
- **덱 화살표**를 하단 → 좌우 가장자리로. 마지막 슬라이드에서만 휠 스크롤 허용(푸터 도달).

### 중복 제거
- 히어로 하단바가 주소·대표·사업자번호·Copyright·로고를 또 찍어 SiteFooter와 이중 노출 → 하단바는 **실시간 진료정보만**(진료상태·시간·대표전화).
- 톡톡 버튼 2개 → 1개(헤더).
- 히어로의 자가체크 버튼 제거(헤더로 이동).

### 모바일 증거 이미지 — 미해결, 내일 최우선
- 원본이 **1383×401 (3.45:1)** 이라 모바일 폭 375px에 맞추면 **109px**밖에 안 됨.
- 시도1 `object-fit:cover` 로 4배 확대 → **좌우 잘림**. 사용자 반려.
- 시도2 `object-fit:fill` 로 세로만 1.71배(2:1 고정) → 잘림 0, 109→186px. 임시 적용 상태.
- **결론: 세로형 이미지를 따로 제작해야 함.** 가로 배너를 늘리든 자르든 타협일 뿐.
  - 데스크톱=가로 원본, 모바일=세로형 별도 소스로 분기하는 구조 필요.
  - 학술 교류 사진은 실제 기록이라 AI 재생성 불가 → **크롭/리컴포지션 또는 재촬영**으로 세로본 제작.

---

## 6. 내일 할 일

0. **모바일 세로형 이미지 제작** ⭐최우선 — 아래 "세로형 이미지" 항목 참고.
1. **치료법 이미지 6장** — codex 토큰 401로 막힘. `codex login` 재시도 필요(실행했으나 auth.json 미갱신 확인).
   - 헬퍼: `/tmp/genimg.sh` (통일 프롬프트 내장). 이미 뽑은 히어로 3장과 톤 맞출 것.
   - ⚠️ **저작권 이미지를 AI로 가공하는 방식은 불가** — 2차적저작물이라 침해 잔존. 처음부터 생성해야 함.
2. **진료시간 확정** — 현재 평일 09:00–18:00 / 토 09:00–13:00 / 점심 13:00–14:00 는 **가정값**.
3. 나머지 탭 콘텐츠 문구 검토(의료광고 심의 관점).
4. 실제 도메인 연결 시 `ALLOW_INDEXING=1` 로 색인 해제.

---

## 7. 함정 기록

- `.hero .shell` 에 `margin-inline:auto` → flex 아이템 stretch 죽어 폭 463px로 붕괴. `width:100%` 필수.
- `--hh` 를 헤더 전체높이 + `.hbar` 높이로 이중 사용 → 순환 계산(헤더 140px). `--barh`/`--hh` 분리함.
- 이미지 `insertBefore(im, frame.firstChild)` → DOM 역순 → **라벨과 사진 불일치**. 순서대로 삽입할 것.
- 자동순환이 사용자 클릭과 경합 → 조작 시 `stopAuto()` 필수.
- Next fetch 캐시(`revalidate`)가 빈 응답을 물고 있어 데이터 이관 후에도 0건 표시 → `.next/cache` 삭제.
- 모바일에서 `.hnav{display:none}` 이라 네비 소실 → `.mnav` 가로 스크롤 스트립 추가.
- 새 Supabase publishable 키(`sb_publishable_`)는 `Authorization: Bearer` 불필요, `apikey` 헤더만. 레거시 JWT(`eyJ`)만 Bearer 필요.
- Vercel Preview 환경변수는 **Git 저장소 연결 필수** → 지금은 Production 환경 사용(색인 차단은 코드 레벨이라 무관).
- Vercel CLI v53은 `env add --value` 미지원.

## 8. 보안

- service_role 키가 소스 주석에 노출된 적 있음 → **폐기 완료(401 확인)**. 커밋·원격 유출 없었음.
- `.env.local` 은 `.gitignore` 처리됨. secret 키는 앞으로도 파일에 적지 말 것.

---

## 9. 세로형 이미지 — 내일 시작점 (사용자 판단)

> "세로형 이미지를 따로 만들어야 할 것 같다" — 맞음. 가로 3.45:1을 모바일에서
> 늘리거나(왜곡) 자르거나(정보 손실) 둘 중 하나라 근본 해결이 안 됨.

### 만들 것 (5장, 세로형)
| 원본 | 내용 | 세로본 제작 방향 |
|---|---|---|
| slide_img4 | Why 삼성 (40,000 Case · 논문 3권) | 논문 표지 + 숫자를 세로로 재조판 |
| slide_img20191004 | 캐더린 깁슨 협진 (4분할 콜라주) | 4분할을 2×2 세로 그리드로 재배치 |
| slide_img3 | 일본 정맥학회장 방문 (인물 4인) | 인물군 크롭 + 캡션 아래 배치 |
| slide_img1 | 리얼스토리(여성) | 인물 중심 세로 크롭 |
| slide_img2 | 리얼스토리(남성) | 인물 중심 세로 크롭 |

- 권장 비율: **4:5 또는 3:4** (모바일 가독 + 인스타 호환)
- ⚠️ 학술 사진은 **실제 기록** → AI 재생성 금지. 크롭·재조판만 허용.
- 텍스트가 박힌 이미지(문구가 이미지 안에 있음)라 크롭 시 문구 잘림 주의.
  → 이미지에서 문구를 빼고 **HTML 텍스트로 올리는 방식**도 검토할 것(SEO에도 유리).

### 구현 방향 (코드)
`src/lib/evidence.ts` 의 `EVIDENCE[]` 에 `imgMobile` 필드 추가 →
`Hero.tsx` 에서 `<picture>` + `media="(max-width:900px)"` 로 분기.
그러면 지금의 `object-fit:fill` 세로 늘림 임시 코드를 제거할 수 있음.
관련 CSS: `globals.css` 맨 끝 "모바일 가운데(증거) 섹션" 블록.

---

## [2026-08-09] 관리자 2차 — 통계 차트 · 진료 캘린더 · 환자 관리

### 들어가기 전에 (필수 2단계)
1. **마이그레이션 실행**: Supabase SQL Editor 에 `supabase/migrations/0003_calendar_patients.sql` 붙여넣고 실행.
   실행 전에는 캘린더·환자 페이지가 테이블 없음으로 실패한다.
2. **공휴일 원천 연결**: 공공데이터포털 "특일 정보" 서비스키를 발급받아
   Vercel 환경변수 `HOLIDAY_API_KEY` 에 넣고 → 캘린더에서 **공휴일 새로고침**.
   키가 없으면 날짜 고정 공휴일 8개만 채워지고(설·추석·부처님오신날·대체공휴일 누락)
   화면 상단에 그 사실이 경고로 뜬다.

### 추가/변경
- **`components/admin/Chart.tsx`** — 의존성 없는 SVG 꺾은선/막대 공용 차트(축·눈금·`<title>` 툴팁).
  클라이언트 JS 0. 기존 `Sparkline.tsx` 는 대체되어 삭제.
- **대시보드** `/admin` — "일별 접속자"(세션 기준) 차트, 기간 7/14/30/90일, 꺾은선↔막대 토글.
  툴팁에 그날 페이지뷰가 함께 나온다.
- **직원 실적** `/admin/staff-activity` — 직원별 처리 건수 막대, 일별 추이(직원별 필터),
  작업 종류 분해, 기간 필터. `activity_logs` 기반.
- **진료 캘린더** `/admin/calendar` — 월 격자(월요일 시작). **토=파랑 / 일·공휴일=빨강**,
  공휴일 이름 표시, 10년(작년~8년 뒤) 이동, 날짜별 예약 건수 + 미내원 표시(주황 점),
  관리자만 보이는 **공휴일 새로고침**(마지막 동기화 시각·실패 연도 표기).
- **당일 패널**(캘린더 하단) — 당일 알림(미내원자), 내원 체크, 당일 특이 기록,
  **주치의**, 다음 진료, 전화번호로 문자 발송, 그 날짜에 예약 추가.
- **환자 관리** `/admin/patients` — 이름·전화 검색, 진료 횟수, 최근 진료, 동명이인 표시.
- **환자 상세** `/admin/patients/[id]` — 진료 이력 누적(최근순), 문자 이력, 환자 상시 메모,
  다음 진료 예정, 동명이인 바로가기.

### 누적 규칙 (요구사항 그대로)
- 한 사람 = **(이름, 전화번호)** 한 쌍. DB 유니크 제약으로 강제.
- 이름이 같아도 전화번호가 다르면 **다른 사람**. 목록·상세에 "동명이인" 표시가 붙는다.
- 전화번호는 숫자만 남겨 저장 — 하이픈 표기 차이로 같은 사람이 갈라지지 않게.

### 스키마 (0003)
- `holidays`(날짜 PK) / `holiday_syncs`(동기화 이력) / `patients`(이름+전화 유니크)
- `appointments` 확장: `patient_id` `arrived_at` `next_at` `day_note` `doctor`
- 내원 체크의 원천은 `arrived_at`. 트리거가 `status` 를 따라 맞춘다.
- `upsert_patient()` RPC — 이름+전화로 찾거나 만든다(직원 이상만).
- 뷰 `v_patient_timeline`(진료+문자 한 줄기) / `v_today_appointments`.

### 보안 — 같이 고친 것
Postgres 뷰는 기본이 **소유자 권한 실행**이라 밑단 테이블의 RLS 를 통과해 버린다.
0002 의 `v_daily_traffic` · `v_click_summary` · `v_staff_activity` 가 그 상태였다
(익명이 조회 가능). 0003 에서 셋 다 `security_invoker = true` 로 바꿨고,
새로 만든 환자 관련 뷰도 처음부터 켜 두었다. **환자 테이블은 직원 이상만**(익명 경로 없음).

### 남은 것
- 문자 실제 발송은 여전히 **큐 적재까지만**(`sms_logs.status = 'queued'`).
  알리고/NHN 등 연동 지점은 `care-actions.ts`의 `sendPatientSms`, `sms/actions.ts`의 `queueSms`.
- 캘린더·환자 화면의 실제 렌더 확인은 마이그레이션 실행 + 관리자 계정 로그인 후에 가능.
- 날짜 계산 자체 점검: `node scripts/test_holidays.mjs`

## [2026-08-09 #2] 관리자·직원 실무 보강 + 검수

### 들어가기 전에
`supabase/migrations/0004_inquiries.sql` 실행 필요. 안 하면 **상담 요청** 페이지만 에러가 뜨고
나머지는 정상 동작한다(직원 홈의 '미처리 문의' 칸은 0으로 표시).

### 추가/변경
- **직원 홈 = 오늘 진료 보드**(`/admin`) — 기존엔 숫자 2개뿐이었다. 오늘 예약자 목록에서 바로
  내원 체크, 미내원 인원, 신규 접수, 미처리 문의, 7일 내 다음 진료 예정까지 한 화면.
- **당일 리마인드 일괄 발송** — 오늘 아직 안 온 예약자 전원에게 안내 문자를 한 번에 큐 적재
  (`sendTodayReminders`). 직원 홈에 버튼.
- **예약 관리 개편** — 이름·전화 검색, **환자 연결** 버튼(patient_id 없는 예약을 이름+전화로
  환자에 붙임), 주치의·내원 여부·당일 기록 노출, 환자 상세 링크. 미연결 건수 경고.
- **상담 요청**(`/admin/inquiries`) — 미처리/전체 필터, 처리 완료 표시, 그 자리에서 문자 발송.
- **사이트 약도 문자 폼이 실제로 접수되게** — `QuickRail.tsx` 가 아무것도 저장하지 않으면서
  "전송 예약했습니다"라고 표시하고 있었다(환자에게 거짓 확인). `inquiries` 로 적재하고
  결과를 받아서 안내하도록 고침. 익명이 문자 발송을 직접 유발하지는 않게 접수까지만 한다.
- **계정 관리 개편** — 권한 필터·검색·권한별 인원, 최근 7일 가입자 '권한 대기' 줄에서
  직원/관리자 한 번에 부여, 지점 변경, **마지막 관리자 잠금 방지**.
- **모바일 대응** — 236px 고정 사이드바가 폰에서 화면을 다 먹었다. 상단 가로 스크롤 바로 전환,
  표는 가로 스크롤, 터치 타깃 40px.

### 검수 방법과 결과
- `scripts/test_holidays.mjs` — 날짜 격자·요일색·10년 범위 자체 검사 통과
- **코드↔DB 대조**: 소스의 모든 `.from(...).select(...)` 를 뽑아 실제 DB에 그대로 질의
  (RLS 로 행이 안 나와도 없는 컬럼이면 PostgREST 가 42703 을 준다) → **32건 중 31건 통과**,
  실패 1건은 `inquiries.kind`(0004 미실행분). `upsert_patient` RPC 존재 확인.
- `npm run build` + `tsc --noEmit` 통과
- 자체 리뷰에서 잡은 결함: 차트 데이터가 전부 0일 때 Y축 눈금 값이 중복돼 React key 충돌
  → 눈금 중복 제거

### 아직 못 한 검수
관리자 화면의 **실제 렌더는 확인하지 못했다**. 로그인이 필요한데 비밀번호를 다룰 수 없어서다.
계정으로 한 바퀴 돌면서 서버 로그를 보면 남은 런타임 결함이 드러난다.

### 렌더 검사 (`node scripts/render_check.cjs`)
관리자 화면은 로그인 뒤에만 보여서 브라우저로 확인하려면 사람이 필요하다. 그래서 앱 코드는
그대로 둔 채 Supabase 클라이언트와 인증만 가짜로 바꿔 끼우고 서버 컴포넌트를 직접 실행한다.
**앱에는 인증 우회가 없다 — 검사 하네스에만 있는 장치다.**

9개 화면 × (데이터 있음 / 없음) = 18회 렌더. 길이만 보지 않고 화면에 나와야 할 것을 단정한다:
캘린더의 토/일 색 클래스와 공휴일 이름, 동명이인 표시, 미연결 예약의 '환자 연결', 권한 대기 줄,
차트 막대·눈금 등. `.render-check/` 로 트랜스파일해서 돌리며 git 에는 올리지 않는다.

## [2026-08-09 #3] 알림창 · 처리자 표시 · 동시 수정 잠금 · 일별 로그

### 들어가기 전에
`0004_inquiries.sql`, `0005_edit_locks.sql` 두 개를 순서대로 실행해야 한다.
실행 전에는 상담 요청·알림 화면과 잠금이 동작하지 않는다(나머지는 정상).

### 추가
- **직원 알림창** `/admin/today` — 대시보드가 '할 일'이라면 여기는 '무슨 일이 있었나'.
  오늘 예약 / 내원 완료 / 대기 / **내가 오늘 한 일** / 오늘 나간 문자 / 미처리 문의를 분리해서 본다.
  '내가 한 일'은 RLS가 직원에게 자기 로그만 주므로 화면 코드에 별도 조건이 필요 없다.
- **처리자 표시** — 예약 관리에 '처리자' 열 추가(마지막으로 저장한 직원 + 시각).
  FK 조인 이름에 기대지 않고 프로필을 따로 읽어 매핑한다(제약 이름이 바뀌어도 안 깨진다).
- **작업 로그** `/admin/logs` (관리자 전용) — 최근 14일 날짜 탭에 건수를 띄우고,
  하루를 골라 시간순으로 본다. 직원별·작업별 필터. 직원 실적이 '얼마나'라면 여기는 '무엇을'.
- **동시 수정 잠금** — 두 사람이 같은 예약을 고치면 나중에 저장한 쪽이 앞사람 입력을 말없이
  덮어쓴다. `edit_locks` 로 3분 잠금을 잡고, 남이 잡고 있으면 **"○○님이 수정 중"** 을 띄우고
  입력·버튼을 비활성화한다. 저장 시점에 잠금을 다시 확인하고 `updated_at` 까지 대조한다
  (잠금 만료 경합은 잠금만으로 못 막는다). 잠금 쓰기는 RPC(security definer)로만 열고
  테이블 직접 쓰기는 정책으로 막았다.

### 검수
- 렌더 검사 확장: 화면 11개 × (데이터 있음/없음) = **22회, 실패 0**.
  잠금 픽스처를 넣어 "○○님이 수정 중" 안내와 비활성 처리가 실제 출력에 나오는지까지 확인.
- 코드↔DB 대조 43건 중 4건 실패 — 전부 아직 실행하지 않은 마이그레이션 때문
  (`edit_locks` 2건 = 0005, `inquiries.kind` 2건 = 0004). 그 외 39건 통과.
- `npm run build`, `tsc --noEmit` 통과.

## [2026-08-09 #4] 이모지 제거 · 설명 도해(SVG)

### 이모지
사이트 규칙(이모지 금지) 위반 2곳을 고쳤다. 저장소 전체를 훑어 **컬러 이모지 7건**을 찾았고,
그중 화면에 나오는 것은 관리자 진료시간·문자 발송 안내의 `⚠️` 2건이었다.
`components/admin/AlertMark.tsx`(SVG)로 바꿨다. 나머지 5건은 `scripts/*.mjs` 의 터미널 출력이라
사이트와 무관해 그대로 뒀다(`🟢🟡🔴`, `✓✗`).
화살표(`→ ← ↗`)는 이모지가 아니라 활자 기호이고 기존 디자인이 쓰던 것이라 유지했다.

### 설명 도해 — `components/Diagram.tsx`
`Slide.diagram` 필드를 추가해 사진 자리에 SVG 도해를 넣을 수 있게 했다.
- `valve` — 정상 판막(위로 흐름) vs 판막 부전(맞닿지 못한 잎 · 역류 · 정맥 확장).
  역류는 사이트 경고색 `#C8102E`.
- `symptoms` — 다리 윤곽에 증상 부위 4곳(허벅지 안쪽 · 무릎 뒤 · 종아리 · 발목) 표시.

래스터 대신 SVG 인 이유는 이 프로젝트가 이미 정한 것과 같다: 아이콘·표식은 SVG 가 정답이고,
선화는 확대해도 안 깨지며 브랜드 색을 그대로 쓴다. ChatGPT 로 생성한 래스터 시안은 구도 참고로만
썼다(그 창의 다운로드가 이 Chrome 프로필에서 파일로 떨어지지 않기도 했다).

### AI 이미지를 만들지 않은 자리 — 이유가 중요하다
`content.ts` 의 빈 이미지 슬롯 17개 중 도해로 채운 것은 2개뿐이다. 나머지는 **만들면 안 된다**:

| 슬롯 | 이유 |
|---|---|
| 합병증 단계별 사진 | 환자 환부 사진. AI 생성은 없는 임상 증거를 지어내는 것 |
| 특수 정맥류 초음파 소견 | 검사 소견. 위와 같음 |
| 절제술 전후 | 전후 사진은 의료광고 심의 대상이기도 하다 |
| 레이저·고주파·경화요법 시술 장면 | 사실적으로 그리면 이 병원의 실제 시술로 읽힌다 |
| 자문의 회의 장면 | 실존 인물 |
| 논문 표지·학회 발표 | 원본을 보유 중 — 정리하면 되고 생성할 이유가 없다 |
| 보험 표 · 진료 절차 · 네트워크 지도 | 이미지보다 HTML/SVG 가 낫다(검색 노출·정확도) |

이 자리들은 **재촬영·원본 정리·HTML 작성**으로 채워야 한다.

### 검수
빌드·타입체크 통과 / 렌더 검사 22회 실패 0 / 코드↔DB 대조 **43건 전건 통과**(0004·0005 반영 확인) /
날짜 계산 자체검사 통과.
